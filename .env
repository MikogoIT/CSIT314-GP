# Environment variables for Docker Compose
# Copy this file to .env and update with your values

# MongoDB Configuration (使用云端数据库)
# MongoDB Atlas连接字符串（如果认证失败，请检查密码和 IP 白名单）
# MONGODB_URI=mongodb+srv://mikogo:msl201215@csit314.9j8jcrg.mongodb.net/csit314?retryWrites=true&w=majority&appName=CSIT314

# 使用本地 Docker MongoDB（临时用于测试）
MONGODB_URI=mongodb://admin:password@mongodb:27017/csit314?authSource=admin
MONGO_ROOT_USER=admin
MONGO_ROOT_PASSWORD=password

# JWT Secret (IMPORTANT: Change this in production!)
JWT_SECRET=CSIT314GROUP-CODESTORM

# Application Environment
NODE_ENV=production

# API Configuration
API_PORT=5000
FRONTEND_PORT=80

# Frontend API URL (使用相对路径，通过nginx代理)
# Docker环境下使用相对路径 /api，开发环境使用 http://localhost:5000/api
REACT_APP_API_URL=/api
